let soma = 0;
contador = 1;

while (soma <= 40) {
    soma += contador;
    console.log (`Somei ${contador} que deu: ${soma}`);
    contador++;
}

console.log (`A quantidade de números somados foi de ${contador}`);